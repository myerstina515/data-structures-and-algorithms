'use strict';


function zipList(LL1, LL2){
  let firstHead = LL1.head;
  let secondHead = LL2.head;
  let currentNode = firstHead.next;
  while (currentNode !== null){
    currentNode = currentNode.next;
  }
}

zipList();
